class Device :
    count = 0
    def __init__(self,ip,name,mac) :
        self.ip = ip
        self.macadress = mac
        self.name = name
        Device.counter += 1
        # result = ping the device

        def get_status(self):
            # return result based on ping result for self.ip
            if result :
                self.status = 'active'
                
            else :
                self.astatus = 'unknown'

        
        

class TV(Device) :
    def Turn_on(self) :
        # connect to self.ip and turn on
        pass
    def Turn_off(self) :
        # connect to self.ip and turn off
        pass


class Prostat(Device) :
    def get_degree(self) :
        # connect to self.id and read degree and return the result
        return result

class smart_TV(TV) :
    def Turn_on(self):
        


mmdreza_tv = TV(51.2.,3
                )